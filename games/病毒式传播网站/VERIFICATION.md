# 验证记录

## 英文版更新

- 页面、弹窗、评分反馈、战绩图片与无障碍标签均改为英文；源码扫描未发现中文用户文案。
- 浏览器验证首页 Copy link 写入纯 URL；即使存在系统分享 API，调用次数也为 0。
- 390 × 844 手机视口无横向溢出，玩法弹窗为英文。
- `npm test`：6 项通过，新增 UTC 跨时区日期验证；JavaScript 语法检查通过。
- 后续浏览器复查遇到工具连接中断，英文结果图下载和手动复制降级未在本轮重新完成浏览器验证。以下为改版前记录。

## 初版

- `npm test`：5 项通过，覆盖理想圆、反向画圆、短笔迹、小圆、半圈、多圈、椭圆、未闭合圆、每日目标与挑战参数。
- `node --check app.js` 和 `node --check server.mjs`：通过。
- Chromium 桌面鼠标实际画圆：99.9 分，结果正常展示。
- 战绩 PNG：浏览器实际触发下载，文件名含本局分数。
- 390 × 844 手机视口：无横向溢出，已检查完整页面截图。
- 模拟 touch pointer 事件：每日挑战得到 97.5 分，对 88.5 分好友正确显示超过 9.0 分。
- 分享 API 使用浏览器内桩验证参数：分数、模式、每日日期均正确；剪贴板拒绝时手动复制框包含正确地址。未向联系人发送消息。
- 重新加载后本机纪录仍保留；重画清除当前结果。
- 浏览器控制台：检查时无错误。

未验证：真实 iOS/Android 系统分享面板、微信容器、公开域名部署。浏览器触摸事件模拟不等同于真机测试。

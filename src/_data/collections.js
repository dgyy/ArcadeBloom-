// Themed selections, validated by scripts/validate-collections.js (ADR-0011).
// Each has slug, title, thesis, 5–12 game slugs, comparisons (one reason per
// game), synthesis, canonicalUrl, socialImage and addedDate. Games must pass
// the directory-content policy. No gameplay evidence records are required.
'use strict';
module.exports = [];

CLAUDE.md
.claude/settings.local.json
.history
.playwright-mcp/

# Eleventy build output
dist/

# Node
node_modules/
npm-debug.log*
test-results/

# Content assets (NOT open source — see CONTENT-LICENSE.md)
# Screenshots belong to respective game authors; downloaded at build time
# via scripts/fetch-screenshots.js, not committed to the repo.
src/static/screenshots/

# Intermediate data files (regenerated by the pipeline)
scripts/js13k-raw.json
scripts/js13k-games.json
scripts/js13k-games-merged.json
scripts/leereilly-raw.json
scripts/candidates/
# Slug allowlist for the Play-click endpoint — emitted by build-catalogue-slugs.js
src/_data/catalogue-slugs.json
# Weekly metrics snapshots — emitted by build-metrics-snapshot.js (baseline is committed)
docs/metrics/snapshots/
# Bluesky post ledger — runtime dedup/cap state (evidence/bluesky-posted.json)
evidence/bluesky-posted.json
